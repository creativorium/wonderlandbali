# Dependencies
node_modules/

# Build output (generated by Vite — rebuild with `npm run build`)
theme/build/
plugin/build/

# Vite
.vite/
*.local

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.*
!.env.example

# Local-only docs (kept out of GitHub by request)
/doc/

# Camera masters dropped in content/ for encoding. The web-ready derivatives
# live in theme/assets/video/ and are the ones that ship.
/content/*.mp4
/content/*.mov

