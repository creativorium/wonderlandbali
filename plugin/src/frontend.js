/**
 * Front-end style entry — pulls in each block's front-end SCSS.
 * Built by Vite -> build/frontend.css (the emitted .js is unused).
 * Add one import line per new block.
 */
import './blocks/hero/style.scss';
